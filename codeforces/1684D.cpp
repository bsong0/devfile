#include <bits/stdc++.h>
using namespace std;

#define INF 0x3f3f3f3f
#define ll long long
#define endl "\n"

const int N = 2*1e5+10;

int cases;
int sums[N], q[N];
int main()
{
    cin.tie(0);
    ios_base::sync_with_stdio(0);
    cout.tie(0);

    cin>>cases;
    while(cases--)
    {
        int n, k, cnt = 0;
        cin>>n>>k;
        for (int i = 0; i<n; i++) cin>>q[i];
        for (int i = 0; i<n; i++)
        {
            if (k > 0 && )   
        }
    }
    return 0;
}